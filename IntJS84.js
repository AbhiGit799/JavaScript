// Optional Chaining (?.) — Safely Access Nested Objects in JavaScript! #coding #dsa


const user = {
    profile:{
        name:"Mohit"
    }
}

// For not getting error we use ?. 
console.log(user?.profile?.city); // undefined

console.log(user?.profile?.name); // Mohit







