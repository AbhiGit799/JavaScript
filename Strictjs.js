
//Below use stict code follows strict rules

"use strict";

var a=10;

console.log(a);


/** 

ES6 modules and class bodies are strict by default. No directive needed.
 
In JavaScript, "use strict"; is a directive introduced in ECMAScript 5 that enables strict mode, 
which enforces stricter rules, prevents silent errors, and improves code reliability. 
It helps catch mistakes like undeclared variables, duplicate parameters, or unsafe assignments, making your code cleaner and more secure.


🔑 What Strict Mode Does
Eliminates silent errors: Converts hidden mistakes (like assigning to read-only properties) into explicit errors.

Prevents undeclared variables: Using a variable without var, let, or const throws a ReferenceError.

Disallows duplicate parameters: Functions cannot have repeated argument names.

Blocks unsafe operations: Forbids deleting variables/functions, using octal literals, or assigning to non-writable properties.

Improves performance: Easier for JavaScript engines to optimize strict code.

Future-proofing: Prevents usage of reserved keywords that may appear in newer ECMAScript versions.

✅ Why Use "use strict";
Improves reliability by catching bugs early.

Enhances maintainability with predictable behavior.

Supports modern JavaScript features like modules and classes.

Encourages best practices by preventing sloppy coding habits.



 * */





























